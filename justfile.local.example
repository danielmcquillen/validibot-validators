# =============================================================================
# Local Configuration for Validibot Validators
# =============================================================================
#
# This file documents the environment variables needed to build and push
# containers to your registry.
#
# SETUP:
#   Add these exports to your shell profile (~/.bashrc, ~/.zshrc, etc.)
#   or to a local .env file that you source before running just commands.
#
# =============================================================================

# Required: Your GCP project ID (for Artifact Registry and Cloud Run)
export VALIDIBOT_GCP_PROJECT="your-gcp-project-id"

# Optional: GCP region (defaults to us-central1)
export VALIDIBOT_GCP_REGION="us-central1"

# =============================================================================
# Usage Examples
# =============================================================================

# Option 1: Set in shell profile for persistent configuration
#   echo 'export VALIDIBOT_GCP_PROJECT="my-project-12345"' >> ~/.zshrc
#   echo 'export VALIDIBOT_GCP_REGION="us-central1"' >> ~/.zshrc
#   source ~/.zshrc

# Option 2: Set temporarily for a single session
#   export VALIDIBOT_GCP_PROJECT="my-project-12345"
#   just build-push energyplus

# Option 3: Inline with the command
#   VALIDIBOT_GCP_PROJECT="my-project-12345" just build-push energyplus

# Option 4: Using just's --set flag
#   just --set gcp_project "my-project-12345" build-push energyplus

# =============================================================================
# Alternative Registries
# =============================================================================

# The default configuration targets Google Artifact Registry. For other
# registries (Docker Hub, GitHub Container Registry, AWS ECR), you'll need
# to modify the justfile's ar_host and ar_repo variables directly, or
# create a fork with your preferred registry configuration.
#
# Examples of what the registry URL looks like:
#
# Google Artifact Registry:
#   us-central1-docker.pkg.dev/my-project/validibot/validibot-validator-energyplus
#
# Docker Hub:
#   docker.io/myorg/validibot-validator-energyplus
#
# GitHub Container Registry:
#   ghcr.io/myorg/validibot-validator-energyplus
#
# AWS ECR:
#   123456789.dkr.ecr.us-east-1.amazonaws.com/validibot-validator-energyplus
