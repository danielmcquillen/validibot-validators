# Core dependencies
pydantic>=2.8.0
httpx>=0.27.0
google-cloud-storage>=2.10.0
google-auth>=2.0.0

# Validibot shared library (from PyPI)
# Pin exact version to bust Docker layer cache on upgrades.
validibot-shared>=0.3.1
