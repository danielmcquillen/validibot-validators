# Core dependencies
pydantic>=2.8.0
httpx>=0.27.0
google-cloud-storage>=2.10.0

# sv_shared from GitHub (update branch/tag as needed)
# For local development, use: pip install -e ../../sv_shared
sv-shared @ git+https://github.com/YOUR_ORG/validibot.git@main#subdirectory=sv_shared

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0
